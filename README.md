## Setup

1. Create a virtual environment with `python -m venv venv`
2. Activate the environment `venv\Scripts\activate` (windows) ro `source venv/bin/activate`
3. Install all dependencies `pip install -r requirements.txt`

### Options
> Add docs folder and rag_db folder in case you encounter an error
